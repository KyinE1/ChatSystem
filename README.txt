Please run the batch file to launch and join the Server.

Alternatively, you may view the .mp4 file to view how multiple users can be used simultaneously to communicate between each other.

chat.zip:
Extract for the application in its entirety.

node_modules directory:
Libraries and modules.

@note: To run locally from the console: node public/start.js
localhost:8080 or 127.0.0.1:8080 (Ctrl + c to exit)